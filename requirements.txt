# Dependências diretas do projeto (transitivas resolvidas pelo pip).
# Dev opcional (LangGraph Studio): pip install "langgraph-cli[inmem]"

# ── Agente (LangGraph + LLM) ─────────────────────────────────────────────────
langchain==1.2.11
langchain-core==1.4.9
langchain-openai==1.1.11
langgraph==1.1.0
langgraph-checkpoint==4.0.1
langgraph-prebuilt==1.0.8   # sem o pin, o pip resolve versão incompatível com langgraph 1.1.0
# Claude opcional: pip install langchain-anthropic  (LLM_PROVIDER=anthropic)

# ── Banco (Supabase / PostgreSQL) ────────────────────────────────────────────
psycopg[binary]==3.3.4
psycopg-pool==3.3.0

# ── Interface (Streamlit) e API (FastAPI) ────────────────────────────────────
streamlit==1.55.0
Authlib==1.7.2            # exigido pelo st.login (Google OAuth)
fastapi==0.135.1
uvicorn==0.41.0

# ── Camada semântica (Cube.js) e utilidades ──────────────────────────────────
PyJWT==2.11.0             # token da API do Cube
PyYAML==6.0.3             # sync_cube_to_db.py lê os cubos .yml
requests==2.32.5
python-dotenv==1.2.2
pandas==2.3.3

# ── Pipeline do banco demo (seed/) ───────────────────────────────────────────
faker==40.31.0            # nomes/CNPJs sintéticos pt_BR
openpyxl==3.1.5           # planilha CMED (xlsx)
